language: node_js
node_js: 10


dist: xenial

before_deploy:
  - npm install
  - npm run fetch
  - npm run build

script:
  echo skip test

deploy:
- provider: pages
  skip_cleanup: true
  keep-history: false
  github-token: $GITHUB_AUTO_PUBLISH_API_TOKEN
  local-dir: build
  on:
    branch: master
    repo: maxime-rainville/travis-dashboard
    condition: -n $RELEASE
